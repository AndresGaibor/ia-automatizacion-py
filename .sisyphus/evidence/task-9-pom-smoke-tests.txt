Task 9: POM Smoke Tests — Evidence
=================================
Date: 2026-04-07

COVERAGE SUMMARY
---------------
New POM smoke tests: tests/integration/test_pom_smoke.py
  - 21 tests PASS
  - Covers: AuthFlow, LoginPage, SessionGuard, ReportsPage,
    CampaignRow, SubscribersPage, FilterSelectorComponent,
    SubscriberRowComponent, SubscribersFlow, SubscribersScraper delegation,
    ReportListingFlow

Fixed broken imports in existing test files:
  - tests/integration/test_scraping_suscriptores.py
  - tests/integration/test_scraping_campanias.py
  - Both now import from src.infrastructure.scraping.* paths
  - 43 tests collected (no import errors)

TEST AREAS COVERED
------------------
1. Auth / Login / Nav:
   - AuthFlow initializes with page + context
   - ScrapingSession initializes with max_retries
   - LoginPage + SessionGuardComponent smoke
   - AuthFlow raises ValueError on missing credentials

2. Reports Page:
   - ReportsPage initializes
   - CampaignRow (formerly CampaignRowComponent) initializes with locator
   - ReportsPage.get_total_pages returns int >= 1
   - ReportListingFlow.execute calls ReportsPage methods

3. Subscribers Page:
   - SubscribersPage initializes
   - FilterSelectorComponent initializes, select_filter returns bool
   - FilterSelectorComponent named methods (select_hard_bounces, etc.) are callable
   - SubscriberRowComponent initializes with locator

4. Subscribers Flow (POM delegation chain):
   - SubscribersFlow initializes + creates POM components
   - extract_filter returns SubscriberFilterResult
   - extract_filter calls filter selector before pagination (the critical fix from task 8)
   - extract_hard_bounces_and_no_abiertos returns CampaignSubscriberReport
   - SubscribersScraper.public_api delegates to SubscribersFlow

5. Report Listing Flow:
   - ReportListingFlow initializes with page + batch_size
   - execute() calls ReportsPage methods in sequence

PYTEST MARKERS USED
-------------------
@pytest.mark.integration — safe for regular runs
@pytest.mark.scraping — marks as scraping tests

No @pytest.mark.slow — all tests are fast mock-based

RUN COMMAND
-----------
uv run pytest tests/integration/test_pom_smoke.py -v

VERIFICATION
------------
21 passed, 150 warnings (pydantic deprecation only) in 0.30s

OLD TESTS FIXED
---------------
43 tests collected from test_scraping_suscriptores.py and
test_scraping_campanias.py after import path corrections.
